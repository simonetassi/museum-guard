# grafana

Grafana provisioning for the MuseumGuard dashboards and the InfluxDB datasource.
Runs as a Docker service (port 3000, admin/admin).

## Run

Started from the project root together with InfluxDB:

```bash
docker-compose up -d grafana
```
